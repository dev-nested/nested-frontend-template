import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
	AppBar: {
		background: '#FFFFFF',
		position: 'static',
	},
    HeroSection:{
        backgroundColor:'#FFFFFF',
        textAlign: 'center',
    },
})